docker-compose up ===> we can also add -d for detach mode
this command createa a network, create volumes specified and runs the services
### docker-compose down :
    shut's down and removes the container, also deletes the network, doesn't remove
    the volumes, if that was needed ==> docker-compose down -v